# 安装和开始使用 done灯 0.4.1

需要 macOS 14.0 或更高版本；Apple Silicon 与 Intel 使用同一个安装包。不需要安装开发工具，也不需要登录 GitHub。

## 安装或升级

1. 升级时，先从菜单栏退出旧版 done灯。
2. 在 [0.4.1 Release](https://github.com/LearnPrompt/done-lamp/releases/tag/v0.4.1) 下载 `done-lamp-0.4.1.dmg`。
3. 双击 DMG，将 `done灯.app` 拖入“应用程序”；如提示替换旧版，选择替换。
4. 从“应用程序”启动，不要直接在 DMG 里运行。
5. 设置页底部应显示 `done灯 0.4.1（11）· 诊断内测版`。

正常升级沿用已有记录和设置。不要再打开 0.3.3 或更早的旧副本：旧版截图清理逻辑可能删除新版仍保留的截图。

## 先体验一次

- 首次引导会请求屏幕录制权限；不准备使用识别时可以拒绝，并继续选“仅记录”。
- 完成一件事后短按桌宠，听到声音并留下完成时间。长按约 2 秒撤销最近记录。
- 在“今天”里编辑记录；在“日报”点击“现在生成”，从“导出与更多”导出 Markdown 或便签图。
- 在“设置”里调整声音、桌宠形象、字体与截图保留时间。

## 开启自动识别

- **模型 API**：选择服务和支持图片的模型，填入自己的 API Key 并测试连接；费用由对应服务收取。
- **Codex Agent（在线）**：安装并登录 Codex，按设置页检测或选择本机 Codex；截图会由 Codex 在线处理。
- **本地模型**：在设置页按需安装或检测 Ollama，下载支持图片的模型，再选择已安装模型；模型下载需要网络、磁盘空间与足够内存。

首次启用识别时，在系统设置中允许 done灯 访问屏幕录制，完全退出再重新打开。模型请求失败时完成时间仍然保留，可手动编辑记录或重试。

截图默认只截取触发时的目标窗口。选择“当前显示器”会包含该显示器的其他可见窗口与桌面。模型 API 与 Codex 会在线处理截图；本地模型通过本机 Ollama 处理。

## 识别失败时导出诊断

无需提前开启日志。打开“设置”并滚动到底部，连续点击版本号 **7 次**（每次间隔不超过 3 秒），点击“导出诊断包”，保存 ZIP 后将文件发给开发者。

诊断默认保留最近 30 天，最多 4000 条 / 4 MiB；不含截图、记录正文、密钥或原始响应，不会自动上传。旧版未记录的错误无法补回。若安装新版后仍失败，按平时操作触发一次，再导出即可。

0.4.1 修复的是通过 Codex CLI 复用第三方服务配置；并未增加任意第三方 API 的直接接入。旧式 profile 等特殊连接配置目前可能提示不支持。

## 常见问题

- **升级后无法截图**：打开“系统设置 → 隐私与安全性 → 屏幕与系统音频录制”，检查 done灯。若以前安装过未正式签名的测试版，移除旧权限条目后重新打开新版授权。
- **桌面出现两个灯**：退出所有旧副本，只从“应用程序”启动新版。
- **系统提示损坏或来源不明**：从上面的官方 Release 重新下载；不必关闭 Gatekeeper 或执行移除隔离命令。若仍失败，请反馈具体提示。
- **反馈问题**：[提交问题](https://github.com/LearnPrompt/done-lamp/issues/new?template=bug_report.yml)，附版本、macOS、机型和步骤，不要附私人截图或密钥。

## 安装包校验

文件：`done-lamp-0.4.1.dmg`，43,025,752 字节。

SHA-256：`f5e6cc54022b20b716c777685cd9d33a8cdf574aae4149bbb390de25d32e559b`

安装包已通过 Developer ID 签名、Apple 公证与装订。系统权限、账号和模型需要在使用者自己的 Mac 上配置。
